import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './LiveTime.jsx'
import './index.css'
import LiveTime from './LiveTime.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <LiveTime />
  </React.StrictMode>,
)
